# Vercelで無料公開ガイド（独自ドメイン対応）

Vercelは**無料プランでも独自ドメインを完全にサポート**しています。このガイドでは、お名前.comのドメインを使ってVercelに公開する手順を詳しく説明します。

## ✅ Vercelの無料プランの特徴

- ✅ **独自ドメイン無制限**（無料プランでも利用可能）
- ✅ **自動SSL証明書**（Let's Encrypt）
- ✅ **無料のCDN配信**（高速）
- ✅ **自動デプロイ**（GitHub連携時）
- ✅ **月100GBの帯域幅**（個人サイトには十分）

## 📋 事前準備

1. GitHubアカウント（推奨）またはGitLab/Bitbucketアカウント
2. お名前.comで取得済みのドメイン
3. ローカルでプロジェクトがビルドできること

---

## 🚀 ステップ1: GitHubにリポジトリを作成

### 1-1. GitHubでリポジトリを作成

1. [GitHub](https://github.com) にログイン
2. 右上の「+」→「New repository」をクリック
3. リポジトリ名を入力（例: `arcle-website`）
4. 「Public」または「Private」を選択（どちらでもOK）
5. 「Create repository」をクリック

### 1-2. ローカルでGitを初期化してプッシュ

ターミナルでプロジェクトのディレクトリに移動して、以下を実行：

```bash
# Gitを初期化（まだ初期化していない場合）
git init

# すべてのファイルをステージング
git add .

# コミット
git commit -m "Initial commit"

# メインブランチに設定
git branch -M main

# GitHubのリポジトリをリモートとして追加
# （YOUR_USERNAMEとYOUR_REPO_NAMEを実際の値に置き換えてください）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# プッシュ
git push -u origin main
```

**注意**: `.env` ファイルや `node_modules` は `.gitignore` に追加しておきましょう。

---

## 🎯 ステップ2: Vercelアカウントを作成

1. [Vercel](https://vercel.com/) にアクセス
2. 「Sign Up」をクリック
3. **「Continue with GitHub」を選択**（推奨）
   - GitHubアカウントでログインすると、自動デプロイの設定が簡単になります
4. 必要に応じて権限を許可

---

## 📦 ステップ3: Vercelにプロジェクトをインポート

### 3-1. プロジェクトを追加

1. Vercelのダッシュボードで「Add New...」→「Project」をクリック
2. 「Import Git Repository」でGitHubリポジトリを選択
3. リポジトリを選択して「Import」をクリック

### 3-2. プロジェクト設定

Vercelは自動的にViteプロジェクトを検出しますが、確認してください：

- **Framework Preset**: `Vite`（自動検出される）
- **Root Directory**: `./`（デフォルトのまま）
- **Build Command**: `npm run build`（自動検出される）
- **Output Directory**: `dist`（自動検出される）
- **Install Command**: `npm install`（自動検出される）

### 3-3. 環境変数の設定（必要な場合）

もし環境変数（`GEMINI_API_KEY`など）を使っている場合：

1. 「Environment Variables」セクションを開く
2. 変数名と値を追加
3. 環境を選択（Production、Preview、Development）

**注意**: 環境変数はビルド時に使用されます。クライアント側で使用する場合は、`VITE_` プレフィックスが必要です。

### 3-4. デプロイ

1. 「Deploy」ボタンをクリック
2. 数分待つとデプロイが完了します
3. デプロイが完了すると、`https://your-project-name.vercel.app` のようなURLが生成されます

---

## 🌐 ステップ4: 独自ドメインを設定

### 4-1. Vercel側でドメインを追加

1. デプロイしたプロジェクトのダッシュボードを開く
2. 「Settings」タブをクリック
3. 左メニューから「Domains」を選択
4. ドメイン入力欄にお名前.comで取得したドメインを入力
   - 例: `example.com` または `www.example.com`
5. 「Add」をクリック

### 4-2. DNS設定の指示を確認

VercelがDNS設定の指示を表示します。以下のような情報が表示されます：

- **ルートドメイン（example.com）用**: AレコードまたはALIASレコード
- **wwwサブドメイン（www.example.com）用**: CNAMEレコード

---

## ⚙️ ステップ5: お名前.comでDNS設定

### 5-1. お名前.comの管理画面にログイン

1. [お名前.com](https://www.onamae.com/) にログイン
2. 「ドメイン設定」をクリック
3. 設定したいドメインを選択

### 5-2. DNSレコード設定を有効化

1. 「DNS関連機能の設定」をクリック
2. 「DNSレコード設定を利用する」を選択
3. 「次へ」をクリック

### 5-3. DNSレコードを追加

#### Aレコード（ルートドメイン用）

Vercelが提供するIPアドレスを設定します。Vercelのドメイン設定画面で確認できますが、通常は以下のいずれかです：

**方法A: Aレコードを使用（推奨）**

- **タイプ**: `A`
- **ホスト名**: `@`（または空白）
- **値（IPアドレス）**: `76.76.21.21`
- **TTL**: `3600`（またはデフォルト値）

**方法B: ALIASレコードを使用（対応している場合）**

一部のDNSプロバイダーではALIASレコードが使えますが、お名前.comでは通常Aレコードを使用します。

#### CNAMEレコード（wwwサブドメイン用）

- **タイプ**: `CNAME`
- **ホスト名**: `www`
- **値**: `cname.vercel-dns.com`
- **TTL**: `3600`（またはデフォルト値）

### 5-4. 設定を保存

1. レコードを追加したら「確認画面へ進む」をクリック
2. 内容を確認して「設定する」をクリック
3. 設定が反映されるまで数時間〜最大48時間かかる場合があります

---

## 🔒 ステップ6: SSL証明書の確認

Vercelは自動的に無料のSSL証明書（Let's Encrypt）を設定します：

1. 「Domains」設定画面で、ドメインの横に「Valid」と表示されるのを確認
2. DNS設定が正しく反映されれば、自動的にSSL証明書が発行されます
3. 数時間かかる場合があります

---

## ✅ ステップ7: 動作確認

### 7-1. DNS設定の確認

DNS設定が反映されているか確認する方法：

**コマンドラインで確認：**
```bash
# ルートドメインの確認
nslookup example.com

# wwwサブドメインの確認
nslookup www.example.com
```

**オンラインツールで確認：**
- [whatsmydns.net](https://www.whatsmydns.net/) でグローバルなDNS設定を確認できます

### 7-2. サイトにアクセス

1. ブラウザで `https://your-domain.com` にアクセス
2. `https://www.your-domain.com` にもアクセスして確認
3. すべてのページが正しく表示されることを確認

---

## 🔄 自動デプロイの設定

GitHubと連携している場合、自動デプロイが有効になっています：

- **mainブランチへのプッシュ**: 本番環境に自動デプロイ
- **他のブランチへのプッシュ**: プレビュー環境に自動デプロイ

### デプロイの確認方法

1. GitHubでコードを変更してプッシュ
2. Vercelのダッシュボードで自動的にデプロイが開始されることを確認
3. デプロイが完了すると、サイトが自動的に更新されます

---

## 🛠️ トラブルシューティング

### DNS設定が反映されない

- **確認**: DNS設定の反映には数時間〜最大48時間かかります
- **確認方法**: `nslookup your-domain.com` で確認
- **解決策**: しばらく待ってから再度確認

### SSL証明書が発行されない

- **確認**: DNS設定が正しく反映されているか確認
- **確認方法**: Vercelの「Domains」設定でエラーメッセージを確認
- **解決策**: DNS設定を確認して、数時間待ってから再度確認

### 404エラーが発生する

- **確認**: `vercel.json` ファイルが正しく設定されているか確認
- **解決策**: プロジェクトのルートに `vercel.json` があることを確認

### ビルドエラーが発生する

- **確認**: ビルドログを確認
- **解決策**: 
  - 環境変数が正しく設定されているか確認
  - `package.json` の依存関係が正しいか確認
  - ローカルで `npm run build` が成功するか確認

---

## 📝 まとめ

1. ✅ GitHubにリポジトリを作成してコードをプッシュ
2. ✅ Vercelアカウントを作成してプロジェクトをインポート
3. ✅ 自動デプロイを確認
4. ✅ Vercelで独自ドメインを追加
5. ✅ お名前.comでDNSレコードを設定
6. ✅ SSL証明書の自動発行を待つ
7. ✅ サイトにアクセスして確認

**Vercelは無料プランでも独自ドメインを完全にサポートしているので、追加費用は一切かかりません！**

---

## 💡 補足情報

### カスタムドメインの追加

複数のドメインやサブドメインを追加することも可能です：
- `example.com`
- `www.example.com`
- `blog.example.com`（別のプロジェクトに設定可能）

### パフォーマンス

Vercelは自動的にCDN（Content Delivery Network）を使用するため、世界中から高速にアクセスできます。

### 月間制限

無料プランでも十分な制限があります：
- 帯域幅: 100GB/月
- ビルド時間: 6000分/月
- 個人サイトには十分すぎる量です

